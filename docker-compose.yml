version: '3'

services:
  yaegar-rest-service:
    build:
      context: .
    image: yaegar-rest-service:latest
    ports:
      - "8888:8080"
    links:
      - mariadb
    depends_on:
      - mariadb

  mariadb:
    image: "mariadb:10.3"
    restart: always
    expose:
      - "3306"
    ports:
    - "3306:3306"
    environment:
      #      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      MYSQL_ROOT_PASSWORD: "secret"
      MYSQL_ROOT_HOST: '%'
